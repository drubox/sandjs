/*
 * 
 */
(function( window, undefined ) {
"use strict";

var sandjs = {};

//Export the sandjs variable to a global scope
window.sandjs = sandjs;

sandjs.debug = {
	status : "Debug",
	version : "1.2"
}
sandjs.ajax = {
	status : "Ajax"
}
})( window );

